My job count: <b><%= @aloc_projects.count %></b><br>
My jobs value: <b><%= to_cur(@aloc_projects.contracted.sum(:quote)) %></b>
<div class="form-wrapper">
	<h4>My Projects</h4>
	<table class="table">
	  <%= render "shared/projects_table" %>
	</table>
</div>
